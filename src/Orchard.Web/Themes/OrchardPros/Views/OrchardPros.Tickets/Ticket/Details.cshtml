@{
    var ticketDetails = New.Ticket_Details(Ticket: Model.Ticket);
    var ticketAside = New.Ticket_Aside(Ticket: Model.Ticket);
    var ticketReplies = New.Ticket_Replies(Ticket: Model.Ticket);
    var ticketSolve = New.Ticket_Solve(Ticket: Model.Ticket);

    Layout.Title = @T("Ticket");
    Layout.Overlays.Add(ticketSolve);
    Layout.Content.Add(ticketDetails);
    Layout.AsideSecond.Add(ticketAside);
    Layout.AfterContent.Add(ticketReplies);
}