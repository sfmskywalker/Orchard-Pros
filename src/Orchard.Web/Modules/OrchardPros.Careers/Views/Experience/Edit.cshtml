@model OrchardPros.Careers.ViewModels.ExperienceViewModel
@{
    Layout.Title = @T("Edit Experience");
}
@using (Html.BeginFormAntiForgeryPost()) {
    @Html.ValidationSummary()
    @Html.EditorForModel()
    <fieldset>
        <button class="primaryAction" type="submit" name="submit.Save" value="submit.Save">@T("Save")</button>
        @Html.ActionLink(T("Cancel").ToString(), "Edit", "Admin", new { Model.Profile.Id, Area = "Orchard.Users" }, new { @class = "button" })
    </fieldset>
}