@using OrchardPros.Careers.Models
@using OrchardPros.Careers.ViewModels
@{
    var profile = (ProfessionalProfilePart) Model.Profile;
    var recommendations = (IList<RecommendationEx>)Model.Recommendations;
}
<fieldset>
    <legend>@T("Recommendations")</legend>
    <table class="items">
        <thead>
            <tr>
                <th>@T("Id")</th>
                <th>@("Recommended By")</th>
                <th>@("Text")</th>
                <th>@("Created")</th>
                <th>@("Approved")</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var recommendation in recommendations) {
                <tr>
                    <td>@recommendation.Id</td>
                    <td>@recommendation.RecommendingUser.UserName</td>
                    <td>@recommendation.Text</td>
                    <td>@recommendation.CreatedUtc</td>
                    <td>@recommendation.Approved</td>
                    <td>
                        @Html.ActionLink(T("Edit").ToString(), "Edit", "Recommendation", new { recommendation.Id, area = "OrchardPros.Careers" }, null) @T("|")
                        @Html.ActionLink(T("Delete").ToString(), "Delete", "Recommendation", new { recommendation.Id, area = "OrchardPros.Careers" }, new { itemprop = "UnsafeUrl RemoveUrl" })
                        @if (!recommendation.Approved) {
                            @T("| ") @Html.ActionLink(T("Approve").ToString(), "Approve", "Recommendation", new {recommendation.Id, area = "OrchardPros.Careers"}, new { itemprop = "UnsafeUrl" })
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @Html.ActionLink(T("Add Recommendation").ToString(), "Create", "Recommendation", new { profile.Id, area = "OrchardPros.Careers" }, new { @class = "button" })
</fieldset>