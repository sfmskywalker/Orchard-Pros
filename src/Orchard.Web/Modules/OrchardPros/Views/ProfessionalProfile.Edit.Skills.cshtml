@using OrchardPros.Models
@{
    var profile = (UserProfilePart) Model.Profile;
    var skills = (IList<Skill>)Model.Skills;
}
<fieldset>
    <legend>@T("Skills")</legend>
    <table class="items">
        <thead>
            <tr>
                <th>@T("Id")</th>
                <th>@("Name")</th>
                <th>@("Rating")</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var skill in skills) {
                <tr>
                    <td>@skill.Id</td>
                    <td>@skill.Name</td>
                    <td>@skill.Rating</td>
                    <td>
                        @Html.ActionLink(T("Edit").ToString(), "Edit", "Skill", new { skill.Id, area = "OrchardPros" }, null) @T(" |")
                        @Html.ActionLink(T("Delete").ToString(), "Delete", "Skill", new { skill.Id, area = "OrchardPros" }, new { itemprop = "UnsafeUrl RemoveUrl" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @Html.ActionLink(T("Add").ToString(), "Create", "Skill", new { profile.Id, area = "OrchardPros" }, new { @class = "button" })
</fieldset>